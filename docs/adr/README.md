# Architecture Decision Records

This directory is for system-wide ADRs.

Create an ADR only when a decision is hard to reverse, surprising without
context, and the result of a real trade-off. Use sequential filenames such as
`0001-full-replay-compatibility-contract.md`.

There are no ADRs recorded yet.
