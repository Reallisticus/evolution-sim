# Optional Mind v2 ML training stack. Simulator runtime inference does not
# import these packages; only trainers such as --trainer torch-actor-critic-bc do.
torch>=2.10,<3
torchrl>=0.12,<1
tensordict>=0.12,<1
gymnasium>=1.2,<2
pettingzoo>=1.25,<2
minari>=0.5,<1
# d3rlpy 2.8.1 pins gymnasium==1.0.0, which conflicts with the
# current Gymnasium/PettingZoo/Minari stack. Keep this compatibility
# hold until d3rlpy releases against newer Gymnasium.
d3rlpy>=2.8,<2.8.1
